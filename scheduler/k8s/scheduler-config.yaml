apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
leaderElection:
  leaderElect: false
profiles:
  - schedulerName: policy-driven-scheduler
    plugins:
      filter:
        enabled:
          - name: WallFilter
      preScore:
        enabled:
          - name: TransferScore
      score:
        enabled:
          - name: TransferScore
            weight: 2
          - name: PropScore
            weight: 1
      postBind:
        enabled:
          - name: WallFilter