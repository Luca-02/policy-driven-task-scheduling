apiVersion: v1
kind: ServiceAccount
metadata:
  name: task-request-controller-service-account
  namespace: task-request-controller
---
# ClusterRole: permissions needed by the controller across the cluster
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: task-request-controller-cluster-role
rules:
  # Read and manage TaskRequests
  - apiGroups: [policydriven.unimi.it]
    resources: [taskrequests]
    verbs: [get, list, watch, patch, update]
  # Update TaskRequest status via the /status subresource
  - apiGroups: [policydriven.unimi.it]
    resources: [taskrequests/status]
    verbs: [get, patch, update]
  # Read GeographicalGroups
  - apiGroups: [policydriven.unimi.it]
    resources: [geographicalgroups]
    verbs: [get, list, watch, patch]
  # Create and watch Jobs.
  - apiGroups: [batch]
    resources: [jobs]
    verbs: [get, list, watch, create, delete, patch]
  # Kopf posts events about its activity
  - apiGroups: [""]
    resources: [events]
    verbs: [create]
  # Kopf uses leases for peer-detection and leader election
  - apiGroups: [coordination.k8s.io]
    resources: [leases]
    verbs: [get, list, watch, create, update, patch, delete]
  # Read CRD definitions to check if the CRD is present before starting the operator
  - apiGroups: [apiextensions.k8s.io]
    resources: [customresourcedefinitions]
    verbs: [get, list, watch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: task-request-controller-cluster-role-binding
subjects:
  - kind: ServiceAccount
    name: task-request-controller-service-account
    namespace: task-request-controller
roleRef:
  kind: ClusterRole
  name: task-request-controller-cluster-role
  apiGroup: rbac.authorization.k8s.io
---
# Role scoped to the compute namespace: allows external users to submit
# TaskRequests. Deliberately excludes create on Jobs and Pods — only the
# controller's service account (via the ClusterRole above) can create Jobs.
# apiVersion: rbac.authorization.k8s.io/v1
# kind: Role
# metadata:
#   name: task-submitter
#   namespace: compute
# rules:
#   - apiGroups: [policydriven.unimi.it]
#     resources: [taskrequests]
#     verbs: [create, get, list, watch, delete]
# taskrequests/status is intentionally omitted: only the controller
# may write the status subresource.
---
# Example RoleBinding — bind to a specific user or group as needed.
# Uncomment and set the subject to grant task-submission access.
#
# apiVersion: rbac.authorization.k8s.io/v1
# kind: RoleBinding
# metadata:
#   name: task-submitter-binding
#   namespace: compute
# subjects:
#   - kind: User
#     name: <username>
#     apiGroup: rbac.authorization.k8s.io
# roleRef:
#   kind: Role
#   name: task-submitter
#   apiGroup: rbac.authorization.k8s.io
