# This is an invalid TaskRequest used to test the validation webhook with external data provider.
# IMPORTANT:
# This task is based on the `dataset-service/data/seed.json` and `context-service/data/seed.json` 
# data seeds.
apiVersion: policydriven.unimi.it/v1alpha1
kind: TaskRequest
metadata:
  name: bad-auth-public
  namespace: compute
spec:
  issuer: issuerpublic # issuer authorized for no contexts
  datasets:
    - d1ctx # dataset with "contexts": {Ford} -> not authorized for issuer-public
